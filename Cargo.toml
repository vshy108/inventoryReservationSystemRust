[package]
name = "inventory_reservation"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
dashmap = "6"
parking_lot = "0.12"
thiserror = "2"
